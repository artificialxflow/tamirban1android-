# 📊 وضعیت فرانت و بک - پروژه تعمیربان

**تاریخ آخرین به‌روزرسانی:** 2025-01-28

---

## 🎨 **وضعیت فرانت اپلیکیشن (Flutter)**

### ✅ کارهای انجام شده

#### 1. زیرساخت اصلی
- ✅ Navigation با GoRouter
- ✅ State Management با Riverpod
- ✅ HTTP Client با Dio (Refresh Token)
- ✅ Error Handling با ApiException
- ✅ Theme و RTL

#### 2. احراز هویت (Auth)
- ✅ صفحه ورود OTP (شماره موبایل)
- ✅ صفحه وارد کردن کد تایید
- ✅ Offline Mode با کد تست `0000`
- ✅ فرمت خودکار شماره موبایل (09xxxxxxxxx)
- ✅ مدیریت توکن و Refresh Token

#### 3. کامپوننت‌های مشترک
- ✅ `AppButton` با 4 variant (Primary, Secondary, Ghost, Danger)
- ✅ استفاده از `AppButton` در صفحه ورود (`login_page.dart`)
  - دکمه "دریافت کد تایید" → `AppButton` (Primary)
  - دکمه "ورود به داشبورد" → `AppButton` (Primary)  
  - دکمه "ارسال مجدد کد" → `AppButton` (Ghost)

#### 4. Package Name
- ✅ تغییر به `ir.tamirban.app` (Android, iOS, macOS)

#### 5. Neshan Map
- ✅ زیرساخت آماده (Permissionها، Maven Repository)
- ✅ کامپوننت اولیه نقشه ایجاد شده
- ⏳ نیاز به SDK >=3.9.0 و API Key

---

## 🔌 **وضعیت بک‌اند (Backend API)**

### ✅ APIهای آماده (طبق مستندات)

#### 1. احراز هویت
- ✅ `POST /api/auth/otp/request` - درخواست کد OTP
- ✅ `POST /api/auth/otp/verify` - تایید کد OTP
- ✅ `POST /api/auth/refresh` - Refresh Token
- ✅ `POST /api/auth/logout` - خروج

#### 2. ماژول‌های اصلی (طبق `sample/todo.md`)
- ✅ Customers: CRUD کامل
- ✅ Visits: CRUD کامل
- ✅ Invoices: CRUD کامل + PDF
- ✅ Marketers: CRUD کامل
- ⏳ SMS Center: در حال توسعه
- ⏳ Reports: در حال توسعه

---

## 🔗 **وضعیت اتصال فرانت به بک**

### ✅ اتصال شده
- ✅ Auth (OTP Login, Refresh Token, Logout)
  - `POST /api/auth/otp/request` ✅
  - `POST /api/auth/otp/verify` ✅
  - `POST /api/auth/refresh` ✅ (خودکار)
  - `POST /api/auth/logout` ✅

### ❌ هنوز اتصال نشده
- ❌ Customers (لیست، ایجاد، ویرایش، حذف)
- ❌ Visits (لیست، ایجاد، ویرایش)
- ❌ Invoices (لیست، ایجاد، ویرایش، PDF)
- ❌ Marketers (لیست، ایجاد، ویرایش)

---

## 📋 **مقایسه فرانت و بک**

| ماژول | بک‌اند | فرانت (Flutter) | وضعیت |
|------|--------|----------------|-------|
| **Auth (OTP)** | ✅ آماده | ✅ کامل | ✅ متصل |
| **Customers** | ✅ آماده | ❌ شروع نشده | ⏳ نیاز به اتصال |
| **Visits** | ✅ آماده | ❌ شروع نشده (کامپوننت نقشه آماده) | ⏳ نیاز به اتصال |
| **Invoices** | ✅ آماده | ❌ شروع نشده | ⏳ نیاز به اتصال |
| **Marketers** | ✅ آماده | ❌ شروع نشده | ⏳ نیاز به اتصال |
| **SMS Center** | ⏳ در حال توسعه | ❌ شروع نشده | ⏳ منتظر بک‌اند |
| **Reports** | ⏳ در حال توسعه | ❌ شروع نشده | ⏳ منتظر بک‌اند |

---

## 🎯 **کارهای باقی‌مانده**

### اولویت 1 (اتصال فرانت به بک)
1. **ماژول Customers**
   - ایجاد Repository و Models
   - اتصال به `GET /api/customers`
   - صفحه لیست مشتریان
   - فرم ایجاد/ویرایش

2. **ماژول Visits**
   - ایجاد Repository و Models
   - اتصال به `GET /api/visits`
   - صفحه لیست ویزیت‌ها
   - فرم ایجاد/ویرایش با نقشه Neshan

3. **ماژول Invoices**
   - ایجاد Repository و Models
   - اتصال به `GET /api/invoices`
   - صفحه لیست پیش‌فاکتورها
   - فرم ایجاد/ویرایش

### اولویت 2 (تکمیل زیرساخت)
4. **کامپوننت‌های مشترک**
   - ✅ Button آماده
   - ❌ Input/TextField
   - ❌ Card
   - ❌ Modal/Dialog
   - ❌ Toast/Notification

5. **Neshan Map**
   - ⏳ به‌روزرسانی SDK
   - ⏳ دریافت API Key
   - ⏳ اتصال کامپوننت به SDK

---

## ✅ **خلاصه**

### فرانت (Flutter):
- ✅ زیرساخت کامل و پایدار
- ✅ احراز هویت کامل
- ✅ کامپوننت Button آماده و استفاده شده
- ❌ ماژول‌های اصلی (Customers, Visits, Invoices) شروع نشده

### بک‌اند:
- ✅ APIهای اصلی آماده است
- ⏳ SMS Center و Reports در حال توسعه

### اتصال:
- ✅ فقط Auth متصل است
- ❌ باقی ماژول‌ها هنوز اتصال نشده‌اند

---

**وضعیت کلی:** فرانت زیرساخت آماده دارد اما فقط Auth به بک متصل است. بقیه ماژول‌ها نیاز به اتصال دارند.

