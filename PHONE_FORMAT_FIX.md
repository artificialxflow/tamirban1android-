# 🔧 رفع مشکل فرمت شماره موبایل و ارسال کد

## ✅ تغییرات انجام شده:

### 1. حذف `prefixText` و استفاده از InputFormatter:
- ❌ **قبل:** `prefixText: '0'` - باعث می‌شد دو تا `0` نمایش داده شود
- ✅ **بعد:** استفاده از `_IranianPhoneFormatter` - فقط یک `0` در اول

### 2. فرمت استاندارد شماره موبایل:
- ✅ **فرمت:** `09123456789` (11 رقم)
- ✅ **InputFormatter:** 
  - فقط اعداد قبول می‌کند
  - اگر با `0` شروع نشده باشد، خودکار `0` اضافه می‌کند
  - جلوگیری از دو تا `0` در اول
  - حداکثر 11 رقم (09xxxxxxxxx)

### 3. استفاده از Development Backend:
- ✅ **تغییر:** `_forceProduction = false`
- ✅ **URL:** `http://localhost:3124/api`
- ✅ **مزیت:** بدون مشکل CORS در development

### 4. بهبود پیام خطا:
- ✅ **CORS Error:** "سرور در دسترس نیست. لطفاً مطمئن شوید که backend در حال اجرا است."
- ✅ **Network Error:** "خطا در اتصال به اینترنت. لطفاً اتصال خود را بررسی کنید."

---

## 📝 فایل‌های تغییر یافته:

1. **`lib/features/auth/presentation/login_page.dart`**:
   - حذف `prefixText: '0'`
   - اضافه شدن `TextInputFormatter` (FilteringTextInputFormatter, LengthLimitingTextInputFormatter, _IranianPhoneFormatter)
   - بهبود پیام خطا

2. **`lib/core/config/app_environment.dart`**:
   - تغییر `_forceProduction` به `false`
   - استفاده از development backend به صورت پیش‌فرض

---

## 🧪 تست:

1. ✅ شماره موبایل باید فقط یک `0` در اول داشته باشد
2. ✅ اگر کاربر `9123456789` وارد کند → تبدیل به `09123456789` می‌شود
3. ✅ جلوگیری از دو تا `0` در اول (00...)
4. ✅ حداکثر 11 رقم قبول می‌شود

---

## ⚠️ نکات:

### برای استفاده:
1. **اجرای Backend:**
   ```bash
   cd sample
   npm run dev
   ```

2. **اگر backend در حال اجرا نیست:**
   - خطای "سرور در دسترس نیست" نمایش داده می‌شود
   - باید backend را اجرا کنید

3. **برای Production:**
   - در `app_environment.dart`:
     ```dart
     static const bool _forceProduction = true;
     ```
   - اما ممکن است CORS error بگیرید مگر اینکه backend origin های مختلف را allow کند

---

## 🔧 کلاس `_IranianPhoneFormatter`:

این کلاس:
- فقط اعداد را قبول می‌کند
- اگر با `0` شروع نشده باشد، خودکار `0` اضافه می‌کند
- جلوگیری از دو تا `0` در اول
- حداکثر 11 رقم (09xxxxxxxxx)

---

**حالا همه چیز درست است! 🚀**

