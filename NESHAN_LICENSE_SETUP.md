# ✅ راهنمای استفاده از فایل License Neshan Map

## 📋 وضعیت فعلی:

### ✅ کارهای انجام شده:
1. ✅ فایل License دانلود شده: `neshan.license`
2. ✅ فایل در مسیر درست قرار گرفته: `assets/licenses/neshan.license`
3. ✅ فایل در `pubspec.yaml` اضافه شده (خط 69)

---

## 🔍 فایل License چیست؟

فایل `neshan.license` یک **فایل مجوز (License)** است که توسط پنل Neshan برای احراز هویت SDK ارائه می‌شود. این فایل شامل اطلاعات مجوز شما برای استفاده از سرویس‌های نقشه نشان است.

### تفاوت بین License و API Key:

| نوع | کاربرد | محل قرارگیری |
|-----|--------|--------------|
| **License File** | احراز هویت SDK | در پوشه `assets/` و باید در کد خوانده شود |
| **API Key** | دسترسی به API سرویس‌ها | در `AndroidManifest.xml` به عنوان meta-data |

---

## ✅ فایل License آماده است!

فایل License شما:
- ✅ دانلود شده
- ✅ در مسیر درست قرار گرفته (`assets/licenses/neshan.license`)
- ✅ در `pubspec.yaml` اضافه شده

---

## 🔧 مراحل بعدی برای استفاده کامل از Neshan Map:

### 1️⃣ **دریافت API Key از پنل Neshan**

از صفحه‌ای که باز کردید (که "TamirBan Android App" را می‌بینید):
- روی دکمه **"دریافت لایسنس" (Get License)** کلیک کنید
- اما برای Flutter، نیاز به **API Key** هم دارید

**در پنل Neshan:**
- باید API Key را هم از همان صفحه دریافت کنید
- یا از بخش تنظیمات کلید دسترسی، API Key را کپی کنید

### 2️⃣ **نصب کتابخانه Neshan Map SDK**

هنوز کتابخانه Neshan Map را نصب نکرده‌اید. باید اضافه شود:

```yaml
# در pubspec.yaml
dependencies:
  neshan_map_sdk: ^1.0.0  # یا آخرین نسخه
```

### 3️⃣ **تنظیم AndroidManifest.xml**

باید API Key و License را در `AndroidManifest.xml` اضافه کنیم:

```xml
<meta-data
    android:name="neshan_api_key"
    android:value="YOUR_API_KEY_HERE" />
```

### 4️⃣ **استفاده از License در کد Flutter**

بعد از نصب SDK، باید فایل License را در کد Flutter بارگذاری کنید.

---

## 📝 چک‌لیست کامل:

- [x] فایل License دانلود شده
- [x] فایل License در `assets/` قرار گرفته
- [x] فایل License در `pubspec.yaml` اضافه شده
- [ ] **API Key از پنل Neshan دریافت شود**
- [ ] **کتابخانه `neshan_map_sdk` نصب شود**
- [ ] **API Key در AndroidManifest.xml اضافه شود**
- [ ] **License در کد Flutter بارگذاری شود**
- [ ] **Permission ها به AndroidManifest.xml اضافه شود**

---

## 🎯 کار بعدی:

**در پنل Neshan:**
1. از صفحه‌ای که باز است (جایی که "TamirBan Android App" را می‌بینید)
2. اگر API Key نمایش داده می‌شود، آن را کپی کنید
3. یا از بخش "ویرایش" (Edit) API Key را پیدا کنید

**سپس به من بگویید:**
- API Key را دارید؟ (مثلاً: `android.xxxxx...`)
- یا باید از جای دیگری دریافت کنید؟

---

## 💡 نکته مهم:

برای استفاده کامل از Neshan Map در Flutter، **هم License و هم API Key** لازم است:
- ✅ License: **دارید** (فایل دانلود شده)
- ❓ API Key: **باید دریافت کنید** (از پنل Neshan)

---

**آیا API Key را هم دریافت کردید؟ اگر دارید، آن را برای من بفرستید تا همه چیز را کامل تنظیم کنم! 🚀**
