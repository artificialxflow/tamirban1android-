# ✅ چک‌لیست Production برای نصب روی گوشی

## 🔍 بررسی تنظیمات فعلی

### ✅ وضعیت: **آماده برای Production**

اپلیکیشن به گونه‌ای تنظیم شده است که:

1. **در موبایل (Android/iOS):**
   - ✅ همیشه از API واقعی استفاده می‌کند: `https://tamirban1.ir/api`
   - ✅ Mock Data **هرگز** استفاده نمی‌شود
   - ✅ کد تست `0000` کار نمی‌کند
   - ✅ بنر "حالت تست" نمایش داده نمی‌شود
   - ✅ باید به اینترنت وصل باشد

2. **در Web (Development):**
   - ⚠️ Offline Mode فعال است (فقط برای توسعه)
   - ⚠️ Mock Data استفاده می‌شود (اگر backend در دسترس نباشد)

---

## 📋 چک‌لیست قبل از ساخت APK

### 1. بررسی تنظیمات Environment

**فایل:** `lib/core/config/app_environment.dart`

- [x] `_forceProduction = false` (باید false باشد)
- [x] `enableOfflineMode` فقط در `development && kIsWeb` فعال است
- [x] در موبایل، همیشه `production` استفاده می‌شود
- [x] API URL در موبایل: `https://tamirban1.ir/api`

### 2. بررسی Mock Data

**فایل‌های استفاده از Mock Data:**
- `lib/features/customers/providers/customers_provider.dart` - ✅ فقط در `enableOfflineMode`
- `lib/features/auth/presentation/login_page.dart` - ✅ فقط در `enableOfflineMode`

**نتیجه:** Mock Data فقط در Web/Development استفاده می‌شود ✅

### 3. بررسی کد تست

**فایل:** `lib/features/auth/presentation/login_page.dart`

- [x] کد `0000` فقط در `enableOfflineMode` کار می‌کند
- [x] در موبایل (که `enableOfflineMode = false` است)، کد `0000` کار نمی‌کند ✅

### 4. بررسی بنرها

**فایل‌ها:**
- `lib/features/customers/presentation/pages/customers_list_page.dart`
- `lib/features/visits/presentation/pages/visits_list_page.dart`

- [x] بنر "حالت تست" فقط در `enableOfflineMode` نمایش داده می‌شود
- [x] در موبایل نمایش داده نمی‌شود ✅

---

## 🚀 ساخت APK برای نصب روی گوشی

### 1. بررسی تنظیمات:
```bash
# بررسی کنید که _forceProduction = false است
# در lib/core/config/app_environment.dart
```

### 2. ساخت Release APK:
```bash
flutter clean
flutter pub get
flutter build apk --release
```

یا برای AAB (برای Google Play):
```bash
flutter build appbundle --release
```

### 3. فایل APK:
```
build/app/outputs/flutter-apk/app-release.apk
```

---

## ✅ تست روی گوشی

### 1. نصب APK:
```bash
adb install build/app/outputs/flutter-apk/app-release.apk
```

### 2. تست‌های مورد نیاز:

#### ✅ تست احراز هویت:
- [ ] شماره موبایل را وارد کنید
- [ ] باید کد واقعی از طریق SMS دریافت کنید
- [ ] کد `0000` **نباید** کار کند ❌
- [ ] بدون اینترنت نباید بتوانید وارد شوید

#### ✅ تست API:
- [ ] لیست مشتری‌ها باید از سرور واقعی دریافت شود
- [ ] داده‌های Mock **نباید** نمایش داده شوند ❌
- [ ] بنر "حالت تست" **نباید** نمایش داده شود ❌
- [ ] لیست ویزیت‌ها باید از سرور واقعی دریافت شود

#### ✅ تست اتصال:
- [ ] با اینترنت: همه چیز باید کار کند
- [ ] بدون اینترنت: باید پیام خطا نمایش داده شود

---

## 🔍 بررسی نهایی

### در موبایل:

1. **API URL:** باید `https://tamirban1.ir/api` باشد ✅
2. **Offline Mode:** باید `false` باشد ✅
3. **Mock Data:** استفاده نمی‌شود ✅
4. **کد تست:** کار نمی‌کند ✅
5. **بنر تست:** نمایش داده نمی‌شود ✅

---

## 📝 یادداشت‌های مهم

1. **نگران نباشید:** Mock Data و کد تست `0000` فقط در Web/Development کار می‌کنند
2. **خودکار:** در موبایل، همه چیز خودکار به production API وصل می‌شود
3. **تغییر ندهید:** `_forceProduction` را تغییر ندهید (باید `false` بماند)

---

## ✅ نتیجه

وقتی APK را روی گوشی نصب می‌کنید:
- ✅ همیشه از API واقعی استفاده می‌شود
- ✅ هیچ Mock Data یا داده فیک نمایش داده نمی‌شود
- ✅ کد تست `0000` کار نمی‌کند
- ✅ باید به اینترنت وصل باشید و backend در دسترس باشد
- ✅ همه چیز واقعی و production-ready است! 🎉
