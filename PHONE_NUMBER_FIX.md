# 🔧 اصلاح ورودی شماره موبایل و رفع مشکل SMS/Login

## ✅ تغییرات انجام شده:

### 1. حذف `+98` از ورودی شماره موبایل:
- ❌ **قبل:** `prefixText: '+98 '`
- ✅ **بعد:** حذف شده

### 2. تغییر Placeholder:
- ❌ **قبل:** `hintText: 'مثال: 09123456789'`
- ✅ **بعد:** `hintText: 'مثال: 9212345678'` (بدون 0 اول)
- ✅ اضافه شدن `helperText` برای راهنمایی کاربر

### 3. نرمال‌سازی خودکار شماره موبایل:
در `_normalizedPhone` getter:
- اگر شماره با `9` شروع شود و 10 رقم باشد → `0` اضافه می‌شود
- اگر شماره با `98` شروع شود و 12 رقم باشد → `98` حذف و `0` اضافه می‌شود
- نتیجه نهایی: `09xxxxxxxxx` (11 رقم) که backend انتظار دارد

### 4. اصلاح فیلد API:
- ❌ **قبل:** `'mobile': mobile` (اشتباه)
- ✅ **بعد:** `'phone': mobile` (درست - backend انتظار `phone` دارد)

---

## 🐛 مشکل اتصال به Backend:

از تصویر می‌بینم که خطای `ERR_CONNECTION_REFUSED` برای `http://localhost:3124` وجود دارد.

### راه‌حل‌ها:

#### گزینه 1: اجرای Backend (توصیه می‌شود):
```bash
cd sample
npm run dev
```

#### گزینه 2: استفاده از Production API:
در `lib/core/config/app_environment.dart`:
```dart
static const bool _forceProduction = true; // تغییر به true
```

⚠️ **نکته:** اگر از production استفاده کنید، ممکن است CORS error بگیرید مگر اینکه backend شما origin شما را allow کرده باشد.

---

## 📝 فایل‌های تغییر یافته:

1. `lib/features/auth/presentation/login_page.dart`:
   - حذف `prefixText: '+98 '`
   - تغییر `hintText` به `'مثال: 9212345678'`
   - اضافه شدن `helperText`
   - بهبود `_normalizedPhone` برای نرمال‌سازی خودکار

2. `lib/data/auth/auth_repository.dart`:
   - تغییر `'mobile'` به `'phone'` در `requestOtp`
   - تغییر `'mobile'` به `'phone'` در `verifyOtp`

---

## 🧪 تست:

1. شماره موبایل را بدون `0` اول وارد کنید (مثلاً: `9212345678`)
2. سیستم خودکار `0` اضافه می‌کند → `09212345678`
3. شماره به فرمت `09xxxxxxxxx` به backend ارسال می‌شود
4. Backend باید در حال اجرا باشد یا از production استفاده کنید

---

**حالا ورودی شماره موبایل اصلاح شده است! 🚀**

