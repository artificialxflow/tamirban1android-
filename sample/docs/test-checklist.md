# چک‌لیست تست کامل پروژه تعمیربان

این چک‌لیست شامل تمام تست‌های لازم برای اطمینان از عملکرد صحیح سیستم است.

## ✅ تست‌های انجام شده

- [x] ورود با OTP (UI)
- [x] ایجاد مشتری از UI
- [x] فیلترها در صفحه Customers
- [x] Pagination در صفحه Customers
- [x] حذف مشتری از UI
- [x] نمایش لیست مشتریان

---

## 🔍 تست‌های باقی‌مانده

### بخش 1: احراز هویت (Authentication)

#### 1.1 تست کامل جریان ورود
- [ ] **درخواست OTP**
  - [ ] شماره موبایل معتبر وارد کنید
  - [ ] بررسی کنید که پیام موفقیت نمایش داده شود
  - [ ] در Development، کد `0000` را دریافت کنید
  - [ ] Rate Limiting: 6 بار متوالی درخواست بفرستید → باید خطای 429 دریافت کنید

- [ ] **تایید OTP و ورود**
  - [ ] کد `0000` را وارد کنید
  - [ ] بررسی کنید که به `/dashboard` هدایت شوید
  - [ ] بررسی کنید که توکن‌ها در `localStorage` ذخیره شده‌اند:
    - `auth_tokens`: شامل `accessToken` و `refreshToken`
    - `auth_user`: اطلاعات کاربر

- [ ] **Refresh Token**
  - [ ] از Postman: درخواست Refresh Token بفرستید
  - [ ] بررسی کنید که توکن جدید دریافت کنید
  - [ ] بررسی کنید که توکن جدید در Environment ذخیره شود

- [ ] **Logout**
  - [ ] از UI: روی "خروج از حساب" کلیک کنید
  - [ ] بررسی کنید که به `/auth` هدایت شوید
  - [ ] بررسی کنید که `localStorage` پاک شده باشد
  - [ ] از Postman: درخواست Logout بفرستید

#### 1.2 تست امنیت
- [ ] **API بدون Token**
  - [ ] `GET /api/customers` بدون Header Authorization
  - [ ] باید خطای `401 Unauthorized` دریافت کنید

- [ ] **Token نامعتبر**
  - [ ] `GET /api/customers` با `Authorization: Bearer invalid_token`
  - [ ] باید خطای `401` با پیام "توکن نامعتبر است" دریافت کنید

- [ ] **Token منقضی شده**
  - [ ] یک Token منقضی شده بسازید (یا صبر کنید)
  - [ ] درخواست API بفرستید
  - [ ] باید خطای `401` با پیام "توکن منقضی شده است" دریافت کنید

---

### بخش 2: Customers API (از Postman)

#### 2.1 CRUD Operations
- [ ] **Create Customer**
  ```http
  POST /api/customers
  Authorization: Bearer <token>
  {
    "displayName": "شرکت تست API",
    "phone": "09123456789",
    "city": "تهران",
    "status": "ACTIVE",
    "tags": ["VIP"]
  }
  ```
  - [ ] بررسی کنید که `status: 201` دریافت کنید
  - [ ] بررسی کنید که `code` تولید شده باشد (مثال: `C-24123`)
  - [ ] بررسی کنید که `_id` و `createdAt` در پاسخ باشد

- [ ] **List Customers**
  ```http
  GET /api/customers
  Authorization: Bearer <token>
  ```
  - [ ] بررسی کنید که ساختار پاسخ درست باشد:
    ```json
    {
      "success": true,
      "data": {
        "data": [...],
        "total": 10,
        "page": 1,
        "limit": 20
      }
    }
    ```

- [ ] **Get Customer Detail**
  ```http
  GET /api/customers/<customer_id>
  Authorization: Bearer <token>
  ```
  - [ ] بررسی کنید که اطلاعات کامل مشتری برگردد
  - [ ] با ID نامعتبر تست کنید → باید `404` دریافت کنید

- [ ] **Update Customer**
  ```http
  PATCH /api/customers/<customer_id>
  Authorization: Bearer <token>
  {
    "displayName": "شرکت تست - ویرایش شده",
    "status": "LOYAL"
  }
  ```
  - [ ] بررسی کنید که داده به‌روزرسانی شود
  - [ ] بررسی کنید که `updatedAt` تغییر کند

- [ ] **Delete Customer**
  ```http
  DELETE /api/customers/<customer_id>
  Authorization: Bearer <token>
  ```
  - [ ] بررسی کنید که مشتری حذف شود
  - [ ] با ID نامعتبر تست کنید → باید `404` دریافت کنید

#### 2.2 فیلترها و Pagination
- [ ] **فیلتر بر اساس وضعیت**
  ```http
  GET /api/customers?status=ACTIVE
  ```
  - [ ] فقط مشتریان با وضعیت ACTIVE برگردانده شوند

- [ ] **فیلتر بر اساس شهر**
  ```http
  GET /api/customers?city=تهران
  ```
  - [ ] فقط مشتریان تهران برگردانده شوند

- [ ] **جستجو**
  ```http
  GET /api/customers?search=تست
  ```
  - [ ] مشتریانی که نام، کد، یا شماره تماس آن‌ها شامل "تست" است برگردانده شوند

- [ ] **Pagination**
  ```http
  GET /api/customers?page=2&limit=10
  ```
  - [ ] `page: 2` و `limit: 10` باشد
  - [ ] حداکثر 10 آیتم در `data` باشد
  - [ ] `total` تعداد کل رکوردها باشد

#### 2.3 Validation Errors
- [ ] **نام کوتاه**
  ```http
  POST /api/customers
  {
    "displayName": "ab",  // کمتر از 3 کاراکتر
    "phone": "09123456789"
  }
  ```
  - [ ] باید `422` با پیام validation error دریافت کنید

- [ ] **شماره موبایل نامعتبر**
  ```http
  POST /api/customers
  {
    "displayName": "شرکت تست",
    "phone": "123"  // نامعتبر
  }
  ```
  - [ ] باید `422` با پیام validation error دریافت کنید

- [ ] **وضعیت نامعتبر**
  ```http
  GET /api/customers?status=INVALID
  ```
  - [ ] باید `422` با پیام "وضعیت وارد شده معتبر نیست" دریافت کنید

---

### بخش 3: UI - صفحه Customers

#### 3.1 فیلترها
- [ ] **جستجو**
  - [ ] متن "تست" را در فیلد جستجو وارد کنید
  - [ ] بررسی کنید که لیست فیلتر شود
  - [ ] بررسی کنید که URL به‌روزرسانی شود (`?search=تست`)

- [ ] **فیلتر وضعیت**
  - [ ] "فعال" را از dropdown انتخاب کنید
  - [ ] بررسی کنید که فقط مشتریان فعال نمایش داده شوند
  - [ ] بررسی کنید که URL به‌روزرسانی شود (`?status=ACTIVE`)

- [ ] **فیلتر شهر**
  - [ ] "تهران" را در فیلد شهر وارد کنید
  - [ ] بررسی کنید که فقط مشتریان تهران نمایش داده شوند
  - [ ] بررسی کنید که URL به‌روزرسانی شود (`?city=تهران`)

- [ ] **پاک کردن فیلترها**
  - [ ] روی دکمه "پاک کردن همه" کلیک کنید
  - [ ] بررسی کنید که همه فیلترها پاک شوند
  - [ ] بررسی کنید که URL پاک شود

#### 3.2 Pagination
- [ ] **صفحه بعدی**
  - [ ] حداقل 25 مشتری ایجاد کنید
  - [ ] روی دکمه "بعدی" کلیک کنید
  - [ ] بررسی کنید که صفحه 2 نمایش داده شود
  - [ ] بررسی کنید که URL به‌روزرسانی شود (`?page=2`)

- [ ] **صفحه قبلی**
  - [ ] در صفحه 2، روی دکمه "قبلی" کلیک کنید
  - [ ] بررسی کنید که صفحه 1 نمایش داده شود

- [ ] **شماره صفحات**
  - [ ] بررسی کنید که شماره صفحات نمایش داده شوند
  - [ ] روی یک شماره صفحه کلیک کنید
  - [ ] بررسی کنید که به آن صفحه بروید

#### 3.3 ایجاد مشتری
- [ ] **فرم کامل**
  - [ ] همه فیلدها را پر کنید
  - [ ] روی "ثبت مشتری" کلیک کنید
  - [ ] بررسی کنید که پیام موفقیت نمایش داده شود
  - [ ] بررسی کنید که فرم پاک شود
  - [ ] بررسی کنید که لیست به‌روزرسانی شود (مشتری جدید در لیست باشد)

- [ ] **Validation در فرم**
  - [ ] نام کوتاه وارد کنید (کمتر از 3 کاراکتر)
  - [ ] شماره موبایل نامعتبر وارد کنید
  - [ ] بررسی کنید که پیام خطا نمایش داده شود

#### 3.4 حذف مشتری
- [ ] **حذف از UI**
  - [ ] روی دکمه "حذف" یک مشتری کلیک کنید
  - [ ] بررسی کنید که مشتری از لیست حذف شود
  - [ ] بررسی کنید که صفحه به‌روزرسانی شود

---

### بخش 4: Session Management

#### 4.1 تمدید خودکار Token
- [ ] **Token منقضی شده**
  - [ ] Token را در `localStorage` منقضی کنید (یا صبر کنید)
  - [ ] یک درخواست API از UI بفرستید (مثلاً فیلتر را تغییر دهید)
  - [ ] بررسی کنید که Token به‌صورت خودکار تمدید شود
  - [ ] بررسی کنید که درخواست موفق باشد

#### 4.2 Auth Guard
- [ ] **دسترسی بدون ورود**
  - [ ] `localStorage` را پاک کنید
  - [ ] به `/dashboard/customers` بروید
  - [ ] باید به `/auth` هدایت شوید

- [ ] **دسترسی بعد از ورود**
  - [ ] وارد شوید
  - [ ] به `/dashboard/customers` بروید
  - [ ] باید صفحه Customers نمایش داده شود

#### 4.3 Logout
- [ ] **خروج از UI**
  - [ ] روی "خروج از حساب" کلیک کنید
  - [ ] بررسی کنید که به `/auth` هدایت شوید
  - [ ] بررسی کنید که `localStorage` پاک شود
  - [ ] بررسی کنید که نتوانید به `/dashboard` دسترسی داشته باشید

---

### بخش 5: Error Handling

#### 5.1 خطاهای API
- [ ] **404 Not Found**
  - [ ] `GET /api/customers/invalid_id`
  - [ ] باید پیام "مشتری یافت نشد" دریافت کنید

- [ ] **422 Validation Error**
  - [ ] داده نامعتبر بفرستید
  - [ ] باید ساختار خطا درست باشد:
    ```json
    {
      "success": false,
      "message": "خطا در اعتبارسنجی داده‌های ورودی",
      "code": "VALIDATION_ERROR",
      "errors": [...]
    }
    ```

- [ ] **500 Internal Server Error**
  - [ ] یک خطای سرور ایجاد کنید (مثلاً MongoDB قطع شود)
  - [ ] باید پیام خطای مناسب نمایش داده شود

#### 5.2 نمایش خطا در UI
- [ ] **خطای شبکه**
  - [ ] اینترنت را قطع کنید
  - [ ] یک عملیات انجام دهید
  - [ ] بررسی کنید که پیام خطای مناسب نمایش داده شود

- [ ] **خطای Validation**
  - [ ] فرم را با داده نامعتبر پر کنید
  - [ ] بررسی کنید که پیام خطا در کنار فیلد مربوطه نمایش داده شود

---

### بخش 6: Performance و UX

#### 6.1 Loading States
- [ ] **Loading در فرم**
  - [ ] فرم ایجاد مشتری را submit کنید
  - [ ] بررسی کنید که دکمه "در حال ثبت..." نمایش داده شود
  - [ ] بررسی کنید که دکمه disabled شود

- [ ] **Loading در لیست**
  - [ ] فیلتر را تغییر دهید
  - [ ] بررسی کنید که یک loading state نمایش داده شود (یا skeleton loader)

#### 6.2 Responsive Design
- [ ] **موبایل**
  - [ ] صفحه را در اندازه موبایل باز کنید (375px)
  - [ ] بررسی کنید که:
    - Sidebar به Drawer تبدیل شود
    - جدول responsive باشد (یا به Card تبدیل شود)
    - فیلترها در یک ستون باشند

- [ ] **تبلت**
  - [ ] صفحه را در اندازه تبلت باز کنید (768px)
  - [ ] بررسی کنید که layout مناسب باشد

#### 6.3 RTL Support
- [ ] **جهت متن**
  - [ ] بررسی کنید که همه متن‌ها RTL باشند
  - [ ] بررسی کنید که اعداد در جهت صحیح باشند (LTR)

- [ ] **آیکون‌ها**
  - [ ] بررسی کنید که آیکون‌ها در جهت صحیح باشند
  - [ ] بررسی کنید که دکمه‌های navigation درست باشند

---

### بخش 7: تست‌های Edge Cases

#### 7.1 داده خالی
- [ ] **لیست خالی**
  - [ ] همه مشتریان را حذف کنید
  - [ ] بررسی کنید که پیام "هیچ مشتری ثبت نشده است" نمایش داده شود

- [ ] **فیلتر بدون نتیجه**
  - [ ] فیلتری اعمال کنید که هیچ نتیجه‌ای نداشته باشد
  - [ ] بررسی کنید که پیام مناسب نمایش داده شود

#### 7.2 داده زیاد
- [ ] **Pagination با داده زیاد**
  - [ ] حداقل 100 مشتری ایجاد کنید
  - [ ] بررسی کنید که Pagination درست کار کند
  - [ ] بررسی کنید که Performance قابل قبول باشد

#### 7.3 کاراکترهای خاص
- [ ] **نام با کاراکترهای خاص**
  - [ ] مشتری با نام "شرکت تست & نمونه" ایجاد کنید
  - [ ] بررسی کنید که درست ذخیره و نمایش داده شود

- [ ] **شماره موبایل با فرمت‌های مختلف**
  - [ ] `09123456789`
  - [ ] `+989123456789`
  - [ ] `9123456789`
  - [ ] بررسی کنید که همه به فرمت یکسان normalize شوند

---

## 📝 گزارش تست

بعد از انجام هر تست، این اطلاعات را ثبت کنید:

- ✅ تست موفق
- ❌ تست ناموفق (با جزئیات خطا)
- ⚠️ نیاز به بررسی بیشتر

### نمونه گزارش:

```
✅ ورود با OTP: موفق
✅ ایجاد مشتری از UI: موفق
❌ Pagination: خطا در صفحه 3 (نمایش داده نمی‌شود)
⚠️ فیلتر شهر: کار می‌کند اما case-sensitive است
```

---

## 🎯 اولویت تست

### اولویت بالا (باید حتماً تست شود):
1. ✅ ورود و خروج
2. ✅ ایجاد/ویرایش/حذف مشتری
3. ✅ فیلترها و Pagination
4. ✅ محافظت از API (بدون Token)
5. ✅ Validation Errors

### اولویت متوسط:
6. Refresh Token
7. Error Handling
8. Session Management
9. Responsive Design

### اولویت پایین:
10. Accessibility
11. Performance با داده زیاد
12. Edge Cases

---

## 🔧 اگر مشکلی پیدا کردید:

1. **Console را بررسی کنید** (F12 → Console)
2. **Network tab را بررسی کنید** (F12 → Network)
3. **لاگ‌های سرور را بررسی کنید** (Terminal)
4. **مستندات را بررسی کنید** (`docs/testing-guide.md`)

---

**آماده برای تست!** 🚀

