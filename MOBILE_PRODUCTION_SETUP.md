# 📱 راهنمای تنظیمات برای نصب روی گوشی (Production)

## ✅ وضعیت فعلی

اپلیکیشن به گونه‌ای تنظیم شده است که:

### 🔴 در موبایل (Android/iOS):
- ✅ **همیشه از API واقعی استفاده می‌شود:** `https://tamirban1.ir/api`
- ✅ **Mock Data غیرفعال است:** هیچ داده فیک نمایش داده نمی‌شود
- ✅ **کد تست `0000` کار نمی‌کند:** فقط احراز هویت واقعی از طریق SMS
- ✅ **بنر "حالت تست" نمایش داده نمی‌شود**
- ✅ **همیشه به اینترنت وصل می‌شود:** باید backend در دسترس باشد

### 🟢 در Web (Development):
- ⚠️ **حالت Offline/Test Mode فعال است:** فقط برای توسعه
- ⚠️ **Mock Data استفاده می‌شود:** اگر backend در دسترس نباشد
- ⚠️ **کد تست `0000` کار می‌کند:** برای تست بدون backend

---

## 🔧 تنظیمات خودکار

### `lib/core/config/app_environment.dart`

```dart
static AppEnvironment get current {
  if (_forceProduction) {
    return AppEnvironment.production;
  }
  
  if (kIsWeb) {
    // Web: استفاده از development
    return AppEnvironment.development;
  }
  // Mobile (Android/iOS): همیشه production
  return AppEnvironment.production;
}

static bool get enableOfflineMode {
  // فقط در development + web
  // در mobile همیشه false است
  return current == AppEnvironment.development && kIsWeb;
}
```

### منطق:
1. **Web Platform (`kIsWeb == true`):**
   - Environment: `development`
   - API URL: `http://localhost:3124/api`
   - Offline Mode: ✅ فعال

2. **Mobile Platforms (Android/iOS) (`kIsWeb == false`):**
   - Environment: `production`
   - API URL: `https://tamirban1.ir/api`
   - Offline Mode: ❌ غیرفعال

---

## 📦 ساخت فایل APK برای نصب روی گوشی

### 1. بررسی تنظیمات:
```bash
# بررسی کنید که _forceProduction = false است
# در lib/core/config/app_environment.dart
```

### 2. ساخت Release APK:
```bash
flutter build apk --release
```

یا برای AAB (برای Google Play):
```bash
flutter build appbundle --release
```

### 3. نصب روی گوشی:
```bash
# APK در مسیر زیر است:
# build/app/outputs/flutter-apk/app-release.apk

# نصب از طریق ADB:
adb install build/app/outputs/flutter-apk/app-release.apk
```

---

## ✅ چک لیست قبل از انتشار

### 1. بررسی API URL:
- [ ] در موبایل باید از `https://tamirban1.ir/api` استفاده کند
- [ ] نه از `http://localhost:3124/api`

### 2. بررسی Offline Mode:
- [ ] در موبایل `enableOfflineMode` باید `false` باشد
- [ ] بنر "حالت تست" نمایش داده نشود
- [ ] Mock Data استفاده نشود

### 3. بررسی احراز هویت:
- [ ] کد تست `0000` کار نکند
- [ ] فقط SMS واقعی از backend دریافت شود

### 4. بررسی اتصال به اینترنت:
- [ ] اپ باید بتواند به `https://tamirban1.ir` وصل شود
- [ ] خطاهای شبکه به درستی نمایش داده شوند

---

## 🧪 تست روی گوشی

### 1. نصب APK:
```bash
flutter build apk --release
adb install build/app/outputs/flutter-apk/app-release.apk
```

### 2. تست احراز هویت:
- ✅ شماره موبایل را وارد کنید
- ✅ باید کد واقعی از طریق SMS دریافت کنید
- ❌ کد `0000` نباید کار کند

### 3. تست API:
- ✅ لیست مشتری‌ها باید از سرور واقعی دریافت شود
- ✅ داده‌های Mock نمایش داده نشود
- ✅ بنر "حالت تست" نمایش داده نشود

---

## 🔍 عیب‌یابی

### مشکل: اپ در موبایل از Mock Data استفاده می‌کند

**بررسی:**
1. چک کنید که `kIsWeb` در موبایل `false` است
2. چک کنید که `AppConfig.current` در موبایل `AppEnvironment.production` است
3. چک کنید که `AppConfig.enableOfflineMode` در موبایل `false` است

### مشکل: اپ نمی‌تواند به API وصل شود

**بررسی:**
1. اینترنت گوشی را چک کنید
2. URL API را چک کنید: باید `https://tamirban1.ir/api` باشد
3. Backend را چک کنید: باید در دسترس باشد
4. Certificate را چک کنید: باید SSL معتبر باشد

---

## 📝 یادداشت‌های مهم

1. **تغییر ندهید `_forceProduction`:** باید `false` بماند تا موبایل خودکار از production استفاده کند

2. **Offline Mode فقط برای Web:** هرگز در موبایل فعال نمی‌شود

3. **Mock Data فقط برای Development:** در production استفاده نمی‌شود

4. **API URL خودکار:** بر اساس platform (web/mobile) انتخاب می‌شود

---

## ✅ نتیجه

وقتی اپ را روی گوشی نصب می‌کنید:
- ✅ همیشه از API واقعی (`https://tamirban1.ir/api`) استفاده می‌شود
- ✅ هیچ Mock Data یا داده فیک نمایش داده نمی‌شود
- ✅ کد تست `0000` کار نمی‌کند
- ✅ باید به اینترنت وصل باشید و backend در دسترس باشد
